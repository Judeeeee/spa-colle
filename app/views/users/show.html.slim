turbo-frame id="modal" data-controller="modal"
  div class="absolute top-4 right-4 bg-white p-4 rounded-lg shadow-lg max-w-sm w-auto z-50" data-modal-target="modal"
    h2 class="text-xl font-bold mb-4" アカウント削除
    p class="text-gray-700"
      | アカウントを削除すると、すべてのデータが失われます。この操作は取り消せません。
    div class="mt-4 flex justify-end"
      = link_to "キャンセル", root_path, class: "px-4 py-2 bg-gray-200 hover:bg-gray-300 rounded", data: { action: "click->modal#close" }
      = button_to "削除する", user_path, method: :delete, class: "px-4 py-2 bg-[#EF454A] hover:bg-[#D73C40] text-white font-bold rounded", data: { turbo_frame: "_top" }
